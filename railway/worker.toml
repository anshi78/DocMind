[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.worker"

[deploy]
startCommand = "arq app.workers.tasks.ingest.WorkerSettings"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
